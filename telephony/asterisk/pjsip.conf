; SIP config for the PSTN transport. Component 17, build step 8.
;
; Empty until then. Two things this will need when written:
;   - external_media_address / external_signaling_address set to the Elastic IP,
;     because EC2 only sees its private address and writes that into SIP
;     messages, which produces a call that connects with no audio
;   - rtp_symmetric on the endpoint, so audio goes back to where it actually
;     came from rather than the address the phone claimed

; TODO
