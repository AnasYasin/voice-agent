# Everything installed into the `voice-agent` conda env.
# Add a line here the moment anything is installed, grouped by the component
# that needs it.
#
#   conda activate voice-agent
#   pip install -r requirements.txt

# --- tooling ---
pytest>=8.0
# The live call path is async end to end, so most of its tests are too.
pytest-asyncio>=1.0
python-dotenv>=1.0

# Pinned to a minor version. tests/test_lint.py asserts the tree is formatted,
# and ruff's formatter output changes between minors, so a looser pin would
# fail the suite on upgrade without any project code changing.
ruff>=0.16,<0.17

# --- config.py: reads defaults.yaml ---
pyyaml>=6.0

# --- component 1: audio converter ---
# ffmpeg is a system binary, not a Python package:
#   sudo apt install ffmpeg

# --- component 2: stt client ---
elevenlabs>=2.63
deepgram-sdk>=7.6
# Scribe v2 realtime is a websocket. The elevenlabs SDK does not pull this in.
websockets>=14.0

# --- component 3: urdu normaliser ---
# stdlib only

# --- component 4: llm client ---
anthropic>=0.40
pydantic>=2.0

# --- component 20: demo server ---
# Already pulled in by livekit-agents, listed because demo.py imports it
# directly and a transitive dependency is not a promise.
aiohttp>=3.9

# --- components 14, 15: transport and startup ---
livekit-agents>=1.6
livekit-plugins-silero>=1.6

# --- component 5: tts client ---
azure-cognitiveservices-speech>=1.40

# --- component 7: call transcripts in postgres ---
asyncpg>=0.30

# --- component 8: call recordings in s3 ---
boto3>=1.34

# --- scripts/roundtrip.py: word error rate for the plumbing test ---
jiwer>=3.0
